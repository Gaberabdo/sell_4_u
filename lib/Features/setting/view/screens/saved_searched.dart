import 'package:flutter/material.dart';
class SavedSearched extends StatelessWidget {
  const SavedSearched({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
